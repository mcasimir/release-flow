module.exports = {
  logLevel: 'debug',
  developmentBranch: 'master',
  plugins: [
    'bump-package-json',
    'generate-changelog',
    => {
      require('child_process').execSync(`npm run build`);
    }
  ]
};
