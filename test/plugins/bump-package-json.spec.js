import assert from 'assert';
import bumpPackageJson from '../../src/plugins/bump-package-json';

describe('plugins', function() {
  describe('bumpPackageJson', function() {
    it.skip('', function() {
      assert(bumpPackageJson);
    });
  });
});
